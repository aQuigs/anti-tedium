{
    "name": "form-filler-extension",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "Browser extension to auto-fill forms and click buttons on registered pages",
    "scripts": {
        "test": "npm run test:unit && npm run test:integration",
        "test:unit": "node --test 'tests/unit/*.test.js'",
        "test:integration": "npx playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.49.0",
        "express": "^4.21.0"
    },
    "volta": {
        "node": "22.22.0"
    }
}
